I've gathered enough from the 129KB replit.md (which is essentially your entire architecture doc), package.json, vite.config, drizzle.config, and FUTURE_SCOPE.md. Let me now create a comprehensive audit document.Prepared architectural assessment for monorepo scalability improvementsPrepared architectural assessment for monorepo scalability improvementsHere's the full audit — 15 issues across 7 categories with prioritized fix paths. Let me highlight the top 5 that matter most right now:
The critical ones you should tackle this week:
1. File tree chaos — LegalNations pages are dumped flat at pages/ root while other verticals have folders. Worse, folder names don't match vertical IDs (sales/ = USDrop, events/ = GoyoTours, admin/ = LBM). Every new page makes this harder to fix. Simple rename + move, 3-4 hours.
2. Vertical ID mismatch — LegalNations has ID hr, USDrop has ID sales, GoyoTours has ID events. This is legacy naming from when it was a generic department portal. Now every verticalId === 'hr' in the codebase actually means LegalNations. Rename once, find-and-replace, done. Gets exponentially harder as more code references these IDs.
3. The components/hr/ naming — DataTable, StatusBadge, FormDialog are universal components used by ALL 17 verticals, but they live in a folder called "hr." This will confuse every developer you onboard. Rename to components/ds/ and merge with components/layout/.
4. Mock data files mix types with data — When you migrate GoyoTours or Finance to Supabase, you need the TypeScript interfaces but not the mock arrays. Right now they're in the same files. Split into types/, constants/, and seeds/ folders so you can delete seed files as verticals go live without losing type definitions.
5. Server is a God File — routes.ts registers every endpoint for every vertical. Before you add HRMS, ATS, CRM, Finance API routes, restructure into routes/{vertical}.routes.ts with an Express Router per vertical.
The foundation you've built — the design system, the DS token object, the universal pages pattern, the schema-per-vertical Supabase approach — is genuinely strong. These fixes are about hardening the structure so it doesn't collapse under its own weight as you scale from 4 live DB verticals to 17.